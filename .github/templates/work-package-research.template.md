# Work Package Research Template

> Use this template to research a proposed work package before package-level requirements and design are finalized. Ground the output in repository evidence, project-level documentation, and the smallest set of relevant implementation surfaces.

## Research scope

- **Work package**: `<./docs/00x-work/...>`
- **Research depth**: `<quick|standard|deep>`
- **Research perspective**: `Senior Software Engineer`
- **Reviewed artifacts**:
  - `docs/...`
  - `src/...`
  - `test/...`

## Executive summary

- **Recommended package direction**: `<one-sentence recommendation>`
- **Overall confidence**: `<high|medium|low>`
- **Top constraints**:
  1. `<constraint>`
  2. `<constraint>`
  3. `<constraint>`

## Upstream context

- `<business or systems-analysis context that materially shapes the package>`

## Repository evidence summary

| Area | Evidence | Implication for the package |
| --- | --- | --- |
| `<area>` | `src/...` / `docs/...` / `test/...` | `<implication>` |

## Recommended approach

### Target outcome

- `<what the package should accomplish>`

### Proposed package boundaries

- **In scope**:
  - `<boundary>`
- **Out of scope**:
  - `<boundary>`

### Key implementation considerations

- `<important consideration>`

## Alternatives considered

| Option | Summary | Pros | Cons | Decision rationale |
| --- | --- | --- | --- | --- |
| `<option>` | `<summary>` | `<pros>` | `<cons>` | `<why accepted or rejected>` |

## Suggested package inputs

- **Candidate traceability references**: `<UCx / SARx / BRx / NFRx / FRx candidates>`
- **Likely touched surfaces**: `<files or modules>`
- **Known validation anchors**: `<tests, commands, or runtime checks>`

## Assumptions and open questions

- `<assumption or open question>`

## Suggested next steps

1. `<next action>`
2. `<next action>`
3. `<next action>`